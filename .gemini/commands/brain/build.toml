description = "Run the brain-build playbook: plan → implement → review → open PR for a GitHub issue or feature description."

prompt = """
Follow this playbook end to end. Planning and review run here in this session; use the
@brain-coder subagent for implementation if available, otherwise implement inline.

@{.claude/skills/brain-build/SKILL.md}

Input: {{args}}
"""
